  Usage for your testserver:
  const client = new OpaqueHttpClient('http://localhost:8080', {
    context: 'hofmann-testserver',
    ksf: argon2idKsf(65536, 3, 1),
  });
  await client.register('alice', 'hunter2');
  const token = await client.authenticate('alice', 'hunter2');

